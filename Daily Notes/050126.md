# 050126 - Daily Work Log

## Decisions
- **Rename:** crate `nimbus` → `kintoun` (Goku's Flying Nimbus, 筋斗雲). Reason: `nimbus` taken on crates.io. GitHub repo already at `mauv0809/kintoun`.
- **License:** dual MIT OR Apache-2.0 (Rust ecosystem norm).
- **Publish v0.0.1 as name reservation** before writing the real impl — to lock the crate name. (`0.0.x` chosen over `0.1.0` to honestly signal pre-alpha.)
- **Directory rename:** `/home/netrom/nimbus` → `/home/netrom/kintoun` (matches crate + GitHub repo).
- **TDD cadence:** one test at a time, full red→green cycle (preferred over batched Phase-1/Phase-2).
- **`ParseError` shape:** four variants tuned to data origin — `Empty` (unit) / `MissingArg(&'static str)` for compile-time labels / `UnknownCommand(String)` for user-input verb / `InvalidAmount { input, reason }` as a struct variant for richer context.
- **Manual `.map_err` over `From<ParseIntError>`** at the `incr` parse site — to capture the bad input from outer scope (which `From` can't reach).

## Meetings & Conversations
-

## Notes
- Confirmed `Incr`/`Decr` already use `by: u64` (memory was stale on the blocker).
- `cargo test` confirmed 5 reds: 4 panic in test body (`todo!()`), 1 (`set`) panics in `from_str` — the only "real" red.
- Publish prep: filled `Cargo.toml` with full metadata (description, dual license, repo, keywords, categories, exclude list); created `LICENSE-MIT` (renamed from `LICENSE`, copyright "Morten Voss") + `LICENSE-APACHE`; rewrote `README.md`. Excluded `.claude/`, `.github/`, `.mcp.json`, `Daily Notes/`, `Scratchpad.md`, `Task Board.md`, `CLAUDE*.md`, `SETUP.md` from the package.
- `cargo publish --dry-run --allow-dirty` succeeded — 11 files, 35 KiB / 11.9 KiB compressed. Compiles cleanly.

## End of Day Summary

### Accomplishments
- **Crate renamed and shipped:** `nimbus` → `kintoun` everywhere (crate name, directory, GitHub repo). `kintoun@0.0.1` published to crates.io as a name reservation. Dual-licensed MIT/Apache-2.0 with both LICENSE files and a real README.
- **Parser landed end-to-end:** `cmd.rs` now compiles and passes **7/7 tests** covering `set`, `get`, `incr` (default + with amount + invalid amount), empty input, and unknown verb. Full red→green TDD discipline, one cycle at a time.
- **Error model expanded organically:** `ParseError` started at `Empty` + `UnknownCommand(String)`, grew to add `MissingArg(&'static str)` and `InvalidAmount { input, reason }` as the parser surfaced new failure modes. Each variant's data shape was chosen deliberately based on data origin.
- **Idioms internalized:** `?` operator + `From` machinery, `ok_or` for `Option → Result`, `map_err` for capturing scope context, struct-variant patterns with `..`, field shorthand in patterns, the bind-then-assert testing pattern.

### Decisions made today
- Renamed crate (`kintoun` over `nimbus`) and directory.
- Dual MIT/Apache-2.0 licensing.
- Published v0.0.1 as name reservation (`0.0.x` over `0.1.0` for honest pre-alpha signal).
- TDD cadence locked: one test at a time, full red→green cycle.
- `ParseError` uses asymmetric variants based on data origin (`&'static str` vs `String` vs struct variant).
- Manual `.map_err` chosen over `From<ParseIntError>` at the `incr` parse site so the bad input gets echoed back in the error.
- `del`/`exists`/`decr` arms intentionally not yet implemented — per per-arm TDD discipline, no test = no impl.

### Open items carried forward
- Hand-roll `Display` + `std::error::Error` for `ParseError` (yesterday's M1 design's "educational pass" — see the trait once before reaching for `thiserror`).
- Backfill `del`/`exists`/`decr` arms with TDD cycles.
- Edge-case parser tests: whitespace runs, casing, trailing args, multi-word values for `set`.
- Move on to `storage.rs` (next module per Layout 2).
- Auto-memory migration: next session start should resolve `~/.claude/projects/-home-netrom-kintoun/`. Old paths at `-home-netrom-nimbus/` and `-home-netrom-learn-rust/` ready for cleanup once verified.

### Tomorrow's priorities (recommended order)
1. **Hand-roll `Display` + `Error` for `ParseError`** — caps off the parser layer, locks in trait literacy. ~20 min.
2. **Backfill `del`/`exists`/`decr` arms** — quick wins, full verb coverage. ~15 min.
3. **Move on to `storage.rs`** — start the next M1 module.

### Reflections / patterns noticed
- Five separate "teachable moments" surfaced today: the `?`-can't-escape-closures rule, inference failures with integer literals + generics + closures, `From`-impl scope limits, asymmetric error variants, patterns-vs-expressions. All were nominated to the knowledge base.
- The decision to publish v0.0.1 *before* writing the real impl was the right call — locking the name removed pressure and the publish prep itself was educational (Cargo metadata, exclude lists, dry-run flow).
- TDD cadence at one-cycle-at-a-time felt natural and produced clean, well-tested code. User wrote test bodies; Claude scaffolded arm impls; review-together caught compile errors and design issues iteratively.
